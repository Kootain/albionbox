- [ ] 新增 SettlementCycle 与 SettlementDetail 两张表，字段满足 spec 中的数据模型要求，并且 migration 可在本地成功应用。
- [ ] 创建结算周期接口能正确锁定 `guild_rankings` 快照 ID（缺失时返回可理解的错误）。
- [ ] 三种结算生成器可独立运行并生成正确明细：力量奖励（阈值/有效值/比例）、力量Top（排名与名额）、资源奖励（四种颜色数量与单价）。
- [ ] 结算详情接口能返回“按玩家聚合”所需的数据结构，前端无需二次猜测即可渲染动态列。
- [ ] 前端可创建结算周期（含配置与两份表格导入），并在创建后进入结算结果页面。
- [ ] “按玩家聚合”视图正确展示：动态列、合计、k/m 单位格式化（两位小数）。
- [ ] 发放状态在“按玩家聚合”视图可一键发放/撤回，并正确记录操作人/时间。
- [ ] 第二视图（按明细列表）可用于核对每条奖励记录。
