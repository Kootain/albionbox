- [x] `VideoRecord` 模型已支持 `globalStartTime` 字段，多语言词条配置完毕。
- [x] 播放器模态框能成功设置视频的全局时间，并且数据能持久化到 IndexedDB，重新打开依然生效。
- [x] 只有在绑定了当前视频全局时间后，“其他视角”列表才能正常工作，且未绑定时会有正确提示。
- [x] “其他视角”列表能够正确过滤出同日期且绑定了全局时间的其他玩家视频。
- [x] 点击列表中的其他玩家后，主视频切换为选中的新玩家视角，并自动 Seek 到了对应的同步时间点；原视频缩小至右下角并静音。
- [x] 播放、暂停、拖动主视频进度条时，右下角的小窗视频能够跟随主视频的进度实时同步（根据各自的全局时间差计算）。
- [x] 再次点击右下角的小窗视频，主副视角成功互换，新的主视频维持当前的全局时间进度，且同步逻辑依然有效。
- [x] 项目能通过 `npm run lint` (`tsc --noEmit`) 检查，无类型报错。