- [x] 页面在检测出重复图片分组后，显示“通知重复作者”的按钮。
- [x] 点击按钮后会弹出 prompt 让用户输入文本，包含预设内容。
- [x] 系统能够对除首发原图以外的重复消息发起回复，并在消息中 `@` 对应的作者（使用 `(met)作者ID(met)` 格式）。
- [x] 若一条消息内有两张图片与之前的图片重复，则该作者只会被通知一次（去重处理）。
- [x] 系统通过 `toast` 正确反馈已通知了多少条消息的作者。