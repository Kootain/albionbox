# Tasks

- [ ] Task 1: 检查 PlayerModal 的视频列表传递与计算
  - [ ] SubTask 1.1: 确认 `PlayerModal.tsx` 中计算 `otherPovs` 的返回值是否被正确应用到了渲染的列表里。
  - [ ] SubTask 1.2: 删除掉无用的 `console.log(v)` 等调试信息。
  - [ ] SubTask 1.3: 如果 `otherPovs` 被错误返回为空，修正过滤条件。