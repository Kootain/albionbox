- [x] 原有的补装申请行被成功抽离为 `RegearApplyRow` 组件，并在多处复用。
- [x] 原有按战斗分组的列表渲染未被破坏，各项功能正常（查看死亡详情、大图、删除、查看KOOK等）。
- [x] 页面过滤条件区域（如状态/频道等）旁边或顶部新增了“按战斗分组”的切换控件（默认开启）。
- [x] 切换开关支持多语言（zh: 按战斗分组, en: Group by Battle）。
- [x] 关闭开关时，页面能够隐藏战斗头部信息，以平铺列表的形式展示所有被筛选的补装申请。
- [x] 平铺视图中的补装申请严格按照 `createTime` 的降序排列（最新在前）。
- [x] 页面在平铺视图和分组视图之间切换时，状态过滤器和频道过滤器的过滤效果均应有效保留。