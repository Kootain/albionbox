- [x] `VideoCard` 长度变小且更紧凑了。
- [x] `VideoCard` 如果没有封面，使用 `username` 代替原来单调的背景。
- [x] `PlayerModal` 在小屏幕下底部的控制按钮不会发生文字竖向换行挤压。
- [x] 确保控制栏能够在极窄屏幕下横向滚动或者折叠显示，不破环播放器整体框架。
- [x] 运行 `npx tsc --noEmit` 没有任何报错。